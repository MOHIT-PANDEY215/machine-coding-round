import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com','i.dummyjson.com',"cdn.dummyjson.com"],
  },
};

export default nextConfig;
